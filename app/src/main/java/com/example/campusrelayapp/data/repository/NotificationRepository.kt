package com.example.campusrelayapp.data.repository

/**
 * Repository responsible for notification-related
 * operations.
 *
 * Notification implementation can later be connected
 * to Firebase Cloud Messaging and/or your API.
 */
class NotificationRepository {

    /**
     * Called when a new notification is received.
     */
    fun handleNotification(
        title: String,
        message: String
    ) {
        // Notification handling will be implemented
        // in the notifications package.
    }
}